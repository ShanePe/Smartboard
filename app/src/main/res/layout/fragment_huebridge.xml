<?xml version="1.0" encoding="utf-8"?>
<GridLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="fill_horizontal|center|fill"
    android:alignmentMode="alignBounds"
    android:animateLayoutChanges="true"
    android:background="@drawable/hue_list_selector"
    android:clickable="true"
    android:columnCount="2"
    android:focusable="true"
    android:layoutMode="clipBounds"
    android:orientation="horizontal"
    tools:orientation="horizontal">

    <ImageView
        android:id="@+id/hue_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="@dimen/text_margin"
        android:contentDescription="@string/img_cnt_device"
        android:padding="2dp"
        app:srcCompat="@drawable/icon_hue_hub" />

    <TextView
        android:id="@+id/hue_ip"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_columnWeight="1"
        android:layout_gravity="center_vertical|fill_horizontal|clip_horizontal|start"
        android:layout_margin="@dimen/text_margin"
        android:gravity="start|fill"
        android:textAppearance="?attr/textAppearanceListItem" />

</GridLayout>
